name: lenskart_movies_app
description: A Flutter app assignment for Lenskart Internship
publish_to: 'none'

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  provider: ^6.0.5
  cached_network_image: ^3.2.3
  fluttertoast: ^8.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
